1. maven project(module) 생성(war)
2. web.xml 추가 (java EE Tools > Geberate Deployment Descriptor Stub)
3. target runtime 설정
4. pom.xml 설정
5. web.xml 설정
	1) /WEB-INF/applicationContext.xml (Root Application Context) 
	2) /WEB-INF/spring0servlet.xml (Web Application Context)
	
6. package
	com.poscodx.mysite.controller
	com.poscodx.mysite.service
	com.poscodx.mysite.repository
	com.poscodx.mysite.vo
	
7. /WEB-INF/assets/*
	/WEB-INF/views/*/*.jsp 정리
8. controller, service, repository 구현 작업
	